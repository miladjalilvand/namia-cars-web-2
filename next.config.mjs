/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["namya.ir","namya3-posts.s3.ir-thr-at1.arvanstorage.ir"], // دامنه‌ای که تصاویر از آن بارگذاری می‌شوند
      },
};

export default nextConfig;
